name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

# XXX: added this comment to allow me to save what was already here.

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between
# the run in-progress and latest queued.
concurrency:
  group: "pages"


jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag irpn
    - name: Create docker container from image
      run: docker create irpn > img_id
    - name: Copy build artifact out of the container
      run: docker cp "$(cat img_id):/opt/irpn/build/" .
    - name: Setup Pages
      id: pages
      uses: actions/configure-pages@v3
    - name: OMG WTF
      run: ls -al
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v1
      with:
        path: |
          index.html
          build/exec/irpn.js

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
